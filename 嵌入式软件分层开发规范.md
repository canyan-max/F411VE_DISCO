# 嵌入式软件分层开发规范

> 本文提炼自嵌入式软件分层架构通用原则，适用于 STM32、GD32、NXP、ESP32 等 MCU 裸机或 RTOS 项目，作为项目开发的分层设计要求。

---

## 一、分层的核心目的

**分层的本质：通过明确职责边界和依赖方向，隔离变化传播，降低维护成本。**

嵌入式项目需要应对四类变化：

| 变化类型 | 典型场景 |
|---------|---------|
| **平台变化** | MCU 更换、HAL 库升级、RTOS 替换、启动文件调整 |
| **板级变化** | GPIO 引脚调整、UART/SPI/I2C 资源变更、DMA 通道变化、中断映射调整 |
| **设备变化** | 传感器型号更换、Flash/EEPROM 替换、显示屏变更、通信模组替换 |
| **业务变化** | 命令定义变化、控制策略升级、状态机调整、业务流程变化 |

分层的终极目标：**让不同类型的问题，在各自边界内闭环处理。**

---

## 二、推荐的七层架构

```
app              ← 应用层，收敛业务变化
  ↓
service / device ← 服务层（通用能力） + 设备层（设备差异）
  ↓
middle           ← 中间件层，大型第三方框架（GUI/文件系统/网络协议栈）
  ↓
bsp              ← 板级支持层，收敛板级资源差异
  ↓
platform         ← 平台层，承接底层平台环境

resource         ← 资源层（产品专属数据：图片/字库/音频/配置）
component        ← 组件层（小型、零依赖的基础能力，横向复用）
```

> 从上到下，越往下越接近硬件；component 和 middle 提供横向复用能力，但体量和依赖程度不同；resource 提供产品专属数据，不参与调用链。

---

## 三、各层职责定义

### 1. app（应用层）

**职责：负责产品业务本身 —— "做什么"**

- 任务调度、命令处理、业务流程、控制策略、状态管理、模式切换、联动逻辑
- **不关心** GPIO 配置、UART 实例、SPI 收发、设备驱动细节

**原则：**
- 只关注业务目标和流程
- 不直接操作 HAL / CubeMX / 寄存器
- 不关心板级细节
- 不承担底层初始化装配职责

---

### 2. service（服务层）

**职责：提供可复用的通用服务能力 —— "公共支撑"**

- 参数管理、通信服务、协议处理（如 Modbus）、存储服务、日志服务、诊断服务、升级服务、告警服务
- **中间件的装配与封装**：负责将 middle 层框架（LVGL、FatFS、LwIP 等）与 bsp 层适配接口进行装配，并对 app 层提供语义化服务接口

**原则：**
- 面向多个业务复用
- 提供稳定服务接口
- 不承载产品策略
- 不直接写具体设备语义
- **中间件初始化在 service 层完成**：bsp 只提供硬件适配回调，service 负责调用 middle 的初始化 API 并注册 bsp 回调，app 只调用 service 接口，不直接感知 middle

---

### 3. device（设备层）

**职责：对具体外部器件进行统一抽象 —— "把器件变成可理解、可替换的设备接口"**

- 温湿度传感器、压力传感器、EEPROM/外部 Flash、LCD/OLED、电机/继电器/蜂鸣器、无线通信模块
- 提供语义化接口：`temp_sensor_read()`、`flash_write_page()`、`motor_set_speed()`
- 屏蔽底层总线访问：不应到处写 `HAL_SPI_Transmit()`、`HAL_I2C_Mem_Read()`

**原则：**
- 屏蔽总线和底层访问细节
- 提供设备语义化接口
- 不写业务流程，不掺杂产品逻辑

---

### 4. middle（中间件层）

**职责：承载大型第三方框架，提供系统级能力 —— "大框架"**

- GUI 框架（LVGL、emWin、u8g2）、文件系统（FatFS、LittleFS）、网络协议栈（LwIP）、加密库（mbedtls）、OTA 框架
- 内部有完整架构，模块间有依赖，需要裁剪和配置
- **被动角色**：middle 本身不主动初始化，由 service 层调用其 API 完成实例化和配置
- **必须通过 bsp 层的适配接口访问硬件**，不直接操作 GPIO/SPI/UART

**原则：**
- 不直接依赖 HAL 或寄存器，通过 bsp 适配层对接硬件
- 对上提供稳定的框架接口（如 LVGL 的 widget API、FatFS 的文件操作 API）
- 需要适配层（display driver、disk I/O、network interface）才能工作
- 可被 device 层封装为更高级的设备抽象

**与 component 的区别：**

| 维度 | component（组件层） | middle（中间件层） |
|------|-------------------|-------------------|
| **体量** | 小，几十到几百行 | 大，几千到几万行 |
| **复杂度** | 单一功能，逻辑简单 | 内部有完整架构 |
| **硬件依赖** | 零依赖，纯算法/数据结构 | 需要适配层对接硬件 |
| **独立性** | 拿来就能用，无需配置 | 需要裁剪、配置、对接接口 |
| **初始化** | 无需初始化，直接调用 | 由 service 层负责初始化和装配 |
| **典型代表** | RingBuffer、CRC、PID | LVGL、FatFS、LwIP、mbedtls |

---

### 5. bsp（板级支持层）

**职责：封装 MCU 片上资源和板级资源 —— "这块板子怎么用"**

- GPIO、UART、SPI、I2C、ADC、PWM、定时器、外部中断、DMA、板级初始化
- 吸收板卡细节：哪个 UART 接哪个外设、GPIO 是否反向、板载 LED 引脚、SPI 是否复用 DMA

**原则：**
- 屏蔽 HAL / CubeMX 细节
- 对上提供稳定板级接口
- 不写设备语义，不写业务逻辑

---

### 6. platform（平台层）

**职责：承接最底层运行环境 —— "地基"**

- HAL、CMSIS、RTOS、启动文件、芯片厂商库、CubeMX 自动生成代码

**原则：**
- 尽量少改
- 不承载业务代码
- 上层不要直接依赖
- 变更控制要谨慎

---

### 7. resource（资源层）

**职责：存放产品专属的数据资源 —— "显示什么内容"**

- UI 图片（logo、图标、背景图、动画帧）
- 字库数据（中文字库、数字字库、符号字库）
- 音频资源（提示音、语音包、音效）
- 配置文件（默认参数表、UI 布局表、多语言文本）
- 其他二进制数据（OTA 包、证书、校准数据）

**原则：**
- **只提供数据，不包含业务逻辑**
- 产品专属，不跨项目复用（与 component 的本质区别）
- 通过头文件暴露数据指针/数组，app 层 `#include` 引用
- 不依赖任何层，是纯数据（编译产物在 .rodata 段）
- 换产品外观只替换 resource，app 代码只需改引用关系

**与 app 的区别：**

| 维度 | app（应用层） | resource（资源层） |
|------|-------------|------------------|
| **内容** | 代码逻辑（C 函数） | 数据（数组、常量表） |
| **职责** | "怎么显示" | "显示什么" |
| **变化** | 业务流程变化 | 外观/内容变化 |
| **编译产物** | 代码段（.text） | 数据段（.rodata） |

---

### 8. component（组件层）

**职责：沉淀与业务无关、与硬件无关的基础能力 —— "可复用能力库"**

- RingBuffer、CRC、PID、状态机框架、软定时器、FIFO、字符串处理、通用算法、内存池、事件分发器

**原则：**
- 与业务无关、与硬件无关
- 可独立测试
- **不反向依赖** app / service / device / bsp / middle / resource
- 可跨多个项目复用

---

## 四、核心设计原则（7 条）

| # | 原则 | 要点 |
|---|------|------|
| 1 | **单一职责** | 一个模块只做一类事，不混职责 |
| 2 | **高内聚、低耦合** | 模块内部聚焦，模块间依赖清晰且尽量少 |
| 3 | **接口清晰** | 通过稳定头文件 API 交互，禁止 `extern` 全局变量穿透；输入输出、生命周期、错误码明确 |
| 4 | **隐藏实现细节** | 上层只关心"做什么"，不关心"怎么做"（I2C/SPI、DMA/轮询等） |
| 5 | **禁止越层访问** | app 不直接调 HAL；service 不直接访问寄存器；device 不越过 bsp 碰 platform |
| 6 | **可替换** | 换 MCU → app 基本不改；换传感器 → 业务不动；换板卡 → device/service 稳定 |
| 7 | **面向接口编程** | 依赖稳定抽象（能力），不依赖具体实现细节 |

---

## 五、运行期调用关系

```
app
  ↓
service / device
  ↓
middle
  ↓
bsp
  ↓
platform
```

- `component` 和 `middle` 都是横向复用层，可被以上各层使用
- `component` 是小型基础能力，`middle` 是大型框架，两者都不是主调用链上的"垂直层"
- `middle` 必须通过 bsp 适配层访问硬件，不能直接操作底层资源
- `resource` 是纯数据层，不参与调用链，被 app 通过 `#include` 引用数据指针

---

## 六、初始化关系 ≠ 运行期调用关系

初始化由 `main` 统一编排，各层只初始化各自职责范围：

```c
main
 ├── bsp_board_init()        ← bsp: 纯硬件配置（GPIO、SPI、UART 等）
 ├── device_init_all()       ← device: 设备驱动初始化
 ├── service_init_all()      ← service: 中间件装配 + 服务初始化
 ├── app_init()              ← app: 业务初始化
 └── app_start()             ← app: 启动业务循环
```

### 中间件初始化流程

中间件（LVGL、FatFS 等）的初始化由 **service 层** 负责，遵循以下流程：

1. **bsp 层**：提供硬件适配回调（如 `bsp_lcd_flush()`、`bsp_disk_read()`）
2. **service 层**：调用 middle 的初始化 API，注册 bsp 回调，完成装配
3. **app 层**：只调用 service 提供的语义接口，不直接感知 middle

**示例（LVGL GUI 服务）：**

```c
// bsp/lcd/bsp_lcd.c - 提供硬件适配回调
void bsp_lcd_flush(lv_disp_drv_t *drv, const lv_area_t *area, lv_color_t *color)
{
    st7735s_draw_pixels(area->x1, area->y1, area->x2, area->y2, (uint16_t*)color);
    lv_disp_flush_ready(drv);
}

// service/gui_service/gui_service.c - 装配中间件
void service_gui_init(void)
{
    lv_init();  // 调用 middle 层 API

    static lv_disp_drv_t disp_drv;
    lv_disp_drv_init(&disp_drv);
    disp_drv.flush_cb = bsp_lcd_flush;  // 注册 bsp 回调
    lv_disp_drv_register(&disp_drv);
}

// app/main_ui/main_ui.c - 只调用 service 接口
void app_init(void)
{
    service_gui_show_temp(25.6);  // 不直接调用 LVGL API
}
```

**关键规则：**
- `app_init()` 不应直接调用 `bsp_board_init()`，避免边界倒置
- `app` 不应直接调用 middle 层 API，通过 service 层间接使用
- `service` 负责 middle 与 bsp 的装配，对 app 屏蔽中间件细节

---

## 七、新模块归属判断（7 问法）

按优先级依次判断：**先看职责 → 再看依赖 → 最后看复用性**

| 判断问题 | 归属层 |
|---------|--------|
| 是业务流程、策略、状态决策？ | **app** |
| 是给多个业务提供公共能力？ | **service** |
| 是在抽象某个具体外部设备？ | **device** |
| 是大型第三方框架（GUI/文件系统/网络协议栈）？ | **middle** |
| 是在封装 MCU 片上资源？ | **bsp** |
| 是官方库、HAL、RTOS、启动文件？ | **platform** |
| 是产品专属的数据资源（图片/字库/音频/配置）？ | **resource** |
| 与业务和硬件都无关、零依赖、可复用？ | **component** |

---

## 八、目录组织规范

每个层内，**每个设备/模块必须拥有独立的子文件夹**，禁止将 `.c` / `.h` 文件直接散落在层目录下。

### 目录结构示例

```
user_file/
── app/
│   └── main_ui/              ← 每个业务模块一个文件夹
│       ├── main_ui.c
│       └── main_ui.h
├── service/
│   └── modbus/               ← 每个服务模块一个文件夹
│       ├── modbus.c
│       └── modbus.h
├── device/
│   ├── st7735s/              ← 每个设备驱动一个文件夹
│   │   ├── st7735s.c
│   │   ── st7735s.h
│   └── dht11/
│       ├── dht11.c
│       └── dht11.h
├── middle/
│   ├── lvgl/                 ← 每个中间件一个文件夹
│   │   ├── lv_conf.h
│   │   └── ...
│   └── fatfs/
│       ├── ff.h
│       └── ...
├── bsp/
│   ├── lcd/                  ← 每个板级资源一个文件夹
│   │   ├── bsp_lcd.c
│   │   └── bsp_lcd.h
│   └── uart/
│       ├── bsp_uart.c
│       └── bsp_uart.h
├── platform/
── resource/                 ← 资源层：产品专属数据
│   ├── images/               ← UI 图片
│   │   ├── logo.c / .h
│   │   └── icon_wifi.c / .h
│   ├── fonts/                ← 字库
│   │   ├── font_cn_16.c / .h
│   │   └── font_num_24.c / .h
│   └── audio/                ← 音频
│       └── beep.c / .h
└── component/
    ├── ring_buffer/
    │   ├── ring_buffer.c
    │   └── ring_buffer.h
    ── crc/
        ├── crc.c
        ── crc.h
```

### CMake 配置要点

`INCLUDE_DIRS` 必须指向子文件夹，使上层通过 `#include "st7735s.h"` 即可引用，无需写路径前缀：

```cmake
idf_component_register(
    SRCS
        "bsp/lcd/bsp_lcd.c"
        "device/st7735s/st7735s.c"
    INCLUDE_DIRS
        "bsp/lcd"
        "device/st7735s"
    REQUIRES
        driver
        log
)
```

### 规则

| 规则 | 说明 |
|------|------|
| **一个模块一个文件夹** | 每个设备/服务/组件独占一个子目录，`.c` 和 `.h` 放在一起 |
| **文件夹命名** | 使用小写 + 下划线，与模块名一致（如 `st7735s`、`bsp_lcd`） |
| **头文件引用** | 通过 CMake `INCLUDE_DIRS` 暴露，上层直接 `#include "xxx.h"` |
| **禁止散落文件** | 层目录下不允许直接出现 `.c` / `.h` 文件 |

---

## 九、总结

> - **app** 负责业务
> - **service** 负责通用服务 + 中间件装配
> - **device** 负责设备抽象
> - **middle** 负责大型第三方框架（GUI/文件系统/网络协议栈）
> - **bsp** 负责板级资源封装
> - **platform** 负责底层平台环境
> - **resource** 负责产品专属数据资源（图片/字库/音频/配置）
> - **component** 负责小型、零依赖的可复用基础组件
>
> **职责要单一，依赖要单向，变化要隔离。**
>
> **component vs middle：** component 是小工具，零依赖，拿来即用；middle 是大框架，需要适配层对接硬件。
>
> **app vs resource：** app 是"怎么显示"的代码逻辑，resource 是"显示什么"的数据内容。换外观只改 resource，换流程只改 app。
